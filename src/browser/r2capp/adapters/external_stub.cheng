type
    ExternalStub = ref
        spec: str
        available: bool
        message: str

fn adapterId(): str =
    return "external-compat"

fn resolve(spec: str): ExternalStub =
    var out: ExternalStub
    new(out)
    out.spec = spec
    out.available = true
    out.message = "compat-redirect-to-native-adapter"
    return out
